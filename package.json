{
  "name": "apimock",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node build/",
    "dev": "nodemon --exec babel-node src/",
    "prebuild": "rm -rf build/",
    "build": "babel src/ -d build/",
    "postbuild": "cp src/apiMockup.json build/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "lulalachen",
  "license": "MIT",
  "dependencies": {
    "babel-cli": "^6.24.1",
    "body-parser": "^1.17.2",
    "chalk": "^2.1.0",
    "express": "^4.15.4",
    "nodemon": "^1.11.0",
    "ramda": "^0.24.1"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1"
  }
}
